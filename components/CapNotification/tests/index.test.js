// import React from 'react';
// import { shallow } from 'enzyme';

// import CapNotification from '../index';

describe('<CapNotification />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
