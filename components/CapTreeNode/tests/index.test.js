// import React from 'react';
// import { shallow } from 'enzyme';

// import CapTreeNode from '../index';

describe('<CapTreeNode />', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
