// import React from 'react';
// import { shallow } from 'enzyme';

// import CapStatisticCard from '../index';

describe('<CapStatisticCard />', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
